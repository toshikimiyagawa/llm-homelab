# Handoff: issue-52-resolver

## Status

Implementation requested.

## Scope

Configure `llm01` so `solvelio.com` lookups use public DNS instead of the router DNS that returns an empty answer for `open-webui.solvelio.com`.

## Files To Use

- `specs/issue-52-resolver/spec.md`
